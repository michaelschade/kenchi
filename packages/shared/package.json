{
  "name": "@kenchi/shared",
  "version": "0.1.0",
  "description": "Utilities shared across all JS packages. Please minimize the number of dependencies.",
  "repository": "git://github.com/michaelschade/kenchi.git",
  "license": "MIT",
  "private": true,
  "scripts": {
    "prepare": "$npm_execpath build",
    "clean": "rm -rf lib",
    "start": "tsc --watch --preserveWatchOutput",
    "build": "tsc",
    "lint": "eslint 'src/**/*.{js,ts,tsx}'"
  },
  "dependencies": {}
}
